apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: db
data:
  my.cnf: |
    [mysqld]
    log-bin=mysql-bin
    binlog_format=row
    server-id=0   # will be overridden per pod
